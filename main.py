from SciDB.scidb_benchmark import SciDB
import os

if __name__ == "__main__":
    ssdb = SciDB(True)
    # while True:
    #     ans = input("Do you wish to generate dummy data [Y/n]?")
    #     if ans.upper() == 'Y' or ans == '':
    #         ssdb = SciDB(True)
    #         break
    #     elif ans.upper() == 'N': break
    
    # os.chdir()